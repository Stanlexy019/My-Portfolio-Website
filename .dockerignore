# Exclude YAML workflow and compose files so they are not sent in the Docker build context
*.yml
*.yaml

# Exclude git and CI metadata
.git
.github

# Common local files / folders that don't belong in the image
node_modules
.vscode
*.log
.DS_Store
Thumbs.db
